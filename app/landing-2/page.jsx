import Landing2View from "../components/landing2-view";

// /landing-2 sigue vivo (para links de campañas) y renderiza la misma landing que el home.
export default function Landing2Page() {
  return <Landing2View />;
}
